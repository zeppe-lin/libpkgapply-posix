# Contributing

Preserve the POSIX mechanism boundary. Semantic application policy belongs in
`libpkgapply`; this repository owns concrete host observation, private staging,
durable mechanism stores, target mutation leases, and POSIX backend
composition.
