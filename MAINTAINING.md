# Maintaining libpkgapply-posix

Release only against a compatible published `libpkgapply` generation. Build
shared and static closures separately and qualify all public headers and POSIX
mechanism behavior before tagging.
